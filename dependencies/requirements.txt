# ===========================================
# Database Dependencies
# ===========================================
pymongo[srv]>=4.6.0    # Already includes bson

# ===========================================
# System and Utility Dependencies
# ===========================================
psutil>=5.9.0
tabulate>=0.9.0
python-dateutil>=2.9.0
pytz>=2025.2

# ===========================================
# Hardware Interface Dependencies
# ===========================================
# GPIO and Hardware Control
RPi.GPIO>=0.7.1
lgpio==0.2.2.0

# Serial Communication for Arduino
pyserial>=3.5

# DHT22 Temperature/Humidity Sensors
adafruit-circuitpython-dht>=4.0.7
adafruit-blinka>=8.32.0

# ===========================================
# Development and Testing
# ===========================================
pytest>=7.0.0
pylint>=3.0.0
black>=24.0.0

# ===========================================
# Installation Notes
# ===========================================
# 1. First uninstall conflicting packages:
#    pip uninstall bson pymongo
#    pip uninstall python-bson  # if installed

# 2. System requirements:
#    - For GPIO: sudo apt install python3-lgpio
#    - For DHT22: sudo apt install libgpiod2

# 3. Install Dependencies:
#    pip install -r requirements.txt

# 4. Run with sudo:
#    sudo python3 <module>-mod-main.py